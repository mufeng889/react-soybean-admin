import { GLOBAL_HEADER_MENU_ID, GLOBAL_SIDER_MENU_ID } from '@/constants/app';

export const headerContainer = document.getElementById(GLOBAL_HEADER_MENU_ID);

export const siderContainer = document.getElementById(GLOBAL_SIDER_MENU_ID);
